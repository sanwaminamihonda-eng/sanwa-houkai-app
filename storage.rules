rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDF報告書ファイル
    match /reports/{reportId}/{fileName} {
      // 認証済みユーザーのみ読み取り可能
      allow read: if request.auth != null;
      // 書き込みはCloud Functionsからのみ（Admin SDK使用）
      allow write: if false;
    }
  }
}
